%p#notice= notice

%p
  %b Name:
  = category.name

%p
  %ol
    - category.products.each_with_index do |product|
      %li
        = product.title
  %br

%p
  %h3 New product
  = render 'products/form'

= link_to 'Edit', edit_category_path(category)
\|
= link_to 'Back', categories_path, class: 'btn btn-primary'
